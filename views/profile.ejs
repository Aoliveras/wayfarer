<div class="container">
    <div class="card">
      <div class="card-content">
      <div class="row">
          <div class="col s6">
              <img src="<%= user.imageUrl %>" style="margin-top: 35px; max-height: 300px;"alt="User Picture"/>
          </div> 
      <div class="col s6">
        <h1><%= user.name %></h1>
        <p>User ID: <%= user._id%></p>
        <p>Joined up: <%= user.createdAt.toDateString() %></p>
        <br>
          <p>Writing from:</p>
          <h4><strong><%= user.currentCity %></strong></h4>
          <a href="/users/profile/edit" class="btn btn-warning btn-sm">Edit Profile</a>
        </div>
      </div>
    </div>
  </div>
  </div>
            <br>
        </p>
    </div>
    <div class="container">
      <div class="card">
        <div class="card-content">
        <h4>See What's new!</h4>
        <a href="/cities" class="btn btn-primary">Let's Explore</a>
      </div>
      </div>
    </div>
<div class="container">
<!-- SAMPLE CONTENT - FOR PLACEMENT ONLY -->
<h2>Travel Journal</h2>
<% posts.forEach(p => { %>
<div class="card" style="margin-bottom: 2rem">
  <div class="card-content">
    <h4 class="section"><%= p.title %></h4>
    <p ><%= p.body %></p>

    <!-- <a href="#" class="section btn btn-primary">See Post</a> -->
 
  </div>
</div>
<% }) %>
</div>